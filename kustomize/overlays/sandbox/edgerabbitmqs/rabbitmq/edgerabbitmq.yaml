apiVersion: smartvillage.computate.org/v1
kind: EdgeRabbitMQ
metadata:
  name: rabbitmq
spec:
  rabbitmq:
    storage:
      enabled: false
    resources:
      limits:
        memory: 400Mi
        cpu: 400m
      requests:
        memory: 400Mi
        cpu: 200m
