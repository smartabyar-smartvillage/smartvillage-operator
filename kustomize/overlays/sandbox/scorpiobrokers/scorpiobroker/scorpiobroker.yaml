apiVersion: smartvillage.computate.org/v1
kind: ScorpioBroker
metadata:
  name: scorpiobroker
spec:
  postgres:
    host: postgres-primary
  kafka:
    enabled: false
  scorpiobroker:
    route:
      create: true
