apiVersion: smartvillage.computate.org/v1
kind: EdgeMongoDB
metadata:
  name: mongodb
spec:
  mongodb:
    replicas: 1
    storage: 1Gi
    resources:
      limits:
        memory: 512Mi
      requests:
        cpu: 100m
