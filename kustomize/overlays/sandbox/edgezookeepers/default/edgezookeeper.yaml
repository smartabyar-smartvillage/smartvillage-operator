apiVersion: smartvillage.computate.org/v1
kind: EdgeZookeeper
metadata:
  name: default
spec:
  zookeeper:
    host: zookeeper
    replicas: 1
    storage: 1Gi
    resources:
      limits:
        memory: 400Mi
        cpu: 400m
      requests:
        memory: 200Mi
        cpu: 100m
