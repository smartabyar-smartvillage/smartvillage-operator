apiVersion: smartvillage.computate.org/v1
kind: EdgeSolr
metadata:
  name: default
  namespace: solr
spec:
  solr:
    name: default
    route:
      create: true
      host_name: ''
    replicas: 1
    storage: 1Gi
    resources:
      limits:
        memory: 2Gi
        cpu: '2'
      requests:
        memory: 1Gi
        cpu: '1'
    configsets:
      - name: computate
        configmap: computate-configset
    collections:
      - name: computate
        configset: computate
      - name: smartvillage
        configset: computate
      - name: trailblazehunt
        configset: computate
  zookeeper:
    replicas: 1
    storage: 1Gi
    limits:
      memory: 500Mi
      cpu: 500m
    requests:
      memory: 200Mi
      cpu: 200m
