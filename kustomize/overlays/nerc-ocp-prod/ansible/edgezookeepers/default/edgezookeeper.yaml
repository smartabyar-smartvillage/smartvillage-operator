apiVersion: smartvillage.computate.org/v1
kind: SmartaByarSmartVillage
metadata:
  name: default
  namespace: smart-village-faeeb6c
spec:
  zookeeper:
    host: zookeeper
    replicas: 1
    resources:
      limits:
        memory: 1Gi
        cpu: '1'
      requests:
        memory: 200Mi
        cpu: 200m
