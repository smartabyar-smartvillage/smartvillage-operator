---
name: smartvillage
description: |
  
schema:
smart_data_model:
  attributes:
    type:
      name: source
      type: string
      description: |
        NGSI Entity type. It has to be {{ ENTITY_TYPE }}
      default: {{ ENTITY_TYPE }}

