metadata:
  labels:
    app.kubernetes.io/instance: demo-orion-ld
    app.kubernetes.io/name: orion-ld
    app.kubernetes.io/version: 1.0.1
    helm.sh/chart: orion-ld-1.0.2
type: Opaque
stringData:
  rabbitmq-password: "{{ lookup('password', '/dev/null length=15 chars=ascii_letters') }}"
  rabbitmq-erlang-cookie: "{{ lookup('password', '/dev/null length=30 chars=ascii_letters') }}"
