apiVersion: postgres-operator.crunchydata.com/v1beta1
kind: PostgresCluster
metadata:
  name: postgres
  namespace: {{ ansible_operator_meta.namespace }}
spec:
  backups:
    pgbackrest:
      image: registry.developers.crunchydata.com/crunchydata/crunchy-pgbackrest:ubi8-2.41-2
      repos:
      - name: repo1
        volume:
          volumeClaimSpec:
            accessModes:
            - "ReadWriteOnce"
            resources:
              requests:
                storage: 1Gi
  instances:
    - dataVolumeClaimSpec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
      replicas: 2
      name: '{{ site_name }}'
  image: registry.developers.crunchydata.com/crunchydata/crunchy-postgres-gis:ubi8-15.1-3.3-0
  postgresVersion: 15
  openshift: true
  postGISVersion: 3.3.0
  users:
    - name: {{ sso.cluster_name }}
      options: "SUPERUSER"
      databases:
        - {{ sso.cluster_name }}
    - name: {{ site_name }}
      options: "SUPERUSER"
      databases:
        - {{ site_name }}
  patroni:
    dynamicConfiguration:
      postgresql:
        pg_hba:
          - "host {{ sso.cluster_name }} {{ sso.cluster_name }} all md5"
          - "local {{ sso.cluster_name }} {{ sso.cluster_name }} trust"
          - "host {{ site_name }} {{ site_name }} all md5"
          - "local {{ site_name }} {{ site_name }} trust"
        